import styled from 'styled-components';

export const Article = styled.article`
  overflow: scroll;
  position: relative;
  width: 100%;
  margin: 10px 0 0 0;
  min-width: 300px;
  white-space: nowrap;
`;
